# Base URL for the backend API.
# When using the Vite dev proxy (default), keep this as /api.
# For production or direct-to-backend dev without the proxy, use the full URL:
#   VITE_API_URL=http://localhost:1000/api
VITE_API_URL=/api
